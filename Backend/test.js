const songs = require('./songsData')

console.log(songs.happy.length)
console.log(songs.sad.length)
console.log(songs.energetic.length)
console.log(songs.chill.length)
console.log(songs.motivational.length)
console.log(songs.healing.length)
console.log(songs.romantic.length)